patch-package
new file mode 120000
--- /dev/null
+++ b/node_modules/react-native-sensors/node_modules/.bin/react-native
@@ -0,0 +1 @@
+../../../react-native/local-cli/wrong-react-native.js
\ No newline at end of file
--- a/node_modules/react-native-sensors/src/rnsensors.js
+++ b/node_modules/react-native-sensors/src/rnsensors.js
@@ -5,11 +5,11 @@ const {
 	Magnetometer: MagnNative
 } = NativeModules;
 
-if (!GyroNative && !AccNative && !MagnNative) {
-	throw new Error(
-		"Native modules for sensors not available. Did react-native link run successfully?"
-	);
-}
+// if (!GyroNative && !AccNative && !MagnNative) {
+// 	throw new Error(
+// 		"Native modules for sensors not available. Did react-native link run successfully?"
+// 	);
+// }
 
 const handle = {
 	Accelerometer: AccNative,
